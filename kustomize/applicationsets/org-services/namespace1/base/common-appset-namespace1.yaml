apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: namespace1-common
  namespace: ocp4-gitops-apps
spec:
  generators:
  - list:
      elements:
      # - cluster: dev
      #   url: https://kubernetes.default.svc
      #   namespace: namespace1
      # - cluster: dev
      #   url: https://kubernetes.default.svc
      #   namespace: namespace1-unstable
  template:
    metadata:
      name: '{{namespace}}-namespace1-common'
    spec:
      source:
        repoURL: https://gitlab.consulting.redhat.com/poc1/gitops-appdev.git
        targetRevision: main
        path: namespace1/common/overlays/{{cluster}}
      project: default
      destination:
        server: '{{url}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true