apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ingestionservice
  namespace: ocp4-gitops-apps
spec:
  generators:
  - list:
      elements:
      # - cluster: dev-unstable
      #   url: https://kubernetes.default.svc
      #   namespace: namespace1-unstable
      # - cluster: dev-stable
      #   url: https://kubernetes.default.svc
      #   namespace: namespace1
  template:
    metadata:
      name: '{{cluster}}-ingestionservice'
    spec:
      source:
        repoURL: https://SFFCU-ITS@dev.azure.com/SFFCU-ITS/IT-SoftwareDevelopment/_git/olb-kustomize
        targetRevision: main
        path: schoolsfirst/services/namespace1/ingestionservice/overlays/{{cluster}}
      project: olb
      destination:
        server: '{{url}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true