apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ingestionservice
  namespace: ocp4-gitops-apps
spec:
  generators:
  - list:
      elements:
      # - cluster: dev-unstable
      #   url: https://kubernetes.default.svc
      #   namespace: namespace1-unstable
      # - cluster: dev-stable
      #   url: https://kubernetes.default.svc
      #   namespace: namespace1
  template:
    metadata:
      name: '{{cluster}}-ingestionservice'
    spec:
      source:
        repoURL: https://gitlab.consulting.redhat.com/poc1/gitops-appdev.git
        targetRevision: main
        path: namespace1/services/namespace1/ingestionservice/overlays/{{cluster}}
      project: default
      destination:
        server: '{{url}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true